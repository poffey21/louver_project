
```
sudo apt-get update
sudo apt-get install build-essential libssl-dev libffi-dev python-dev python-pip virtualenv

virtualenv ~/.venvs/crossbar
. ~/.venvs/crossbar/bin/activate

pip install crossbar
```
